name: Apply UI remediation

on:
  push:
    branches:
      - ui/comprehensive-review-and-improvements

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ui/comprehensive-review-and-improvements
      - name: Apply reviewed source changes
        run: python scripts/apply-ui-remediation.py
      - name: Review generated diff
        run: |
          git diff --check
          git diff --stat
      - name: Commit remediation
        run: |
          rm .github/workflows/apply-ui-remediation.yml scripts/apply-ui-remediation.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/workflows/ci.yml UI_IMPROVEMENT_IMPLEMENTATION_PLAN.md components e2e lib tests .github/workflows/apply-ui-remediation.yml scripts/apply-ui-remediation.py
          git commit -m "fix(ui): guard capability and modal state transitions"
          git push origin HEAD:ui/comprehensive-review-and-improvements
